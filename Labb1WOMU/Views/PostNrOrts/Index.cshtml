@model IEnumerable<Labb1WOMU.Models.PostNrOrt>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ort)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ort)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PostNr }) |
            @Html.ActionLink("Details", "Details", new { id=item.PostNr }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PostNr })
        </td>
    </tr>
}

</table>
